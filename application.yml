spring:
  datasource:
    url: jdbc:mysql://localhost:3306/practice
    username: root
    password: Razdeepak12#
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
